# User Service (Bus Booking)

## Quick start (local)

Prerequisites:
- Java 17
- Maven
- MongoDB running on `mongodb://localhost:27017` (or Docker)

Build & run:
```bash
mvn clean package
mvn spring-boot:run
